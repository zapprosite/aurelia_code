---
description: Transfere a execução de implementação para o OpenCode.
---
# Workflow: handoff-to-opencode

1. **Setup**: Garante que o OpenCode está operando dentro da worktree correta.
2. **Comando**: Invoca o OpenCode no diretório da worktree com o contrato da slice.
3. **Auto-Approve**: Permite modo auto (Tier B) se dentro da worktree isolada.
4. **Sincronia**: Atualiza o plano de execução com o resultado do OpenCode.
