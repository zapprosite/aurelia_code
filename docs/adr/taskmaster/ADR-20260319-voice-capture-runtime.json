{
  "adr_id": "ADR-20260319-voice-capture-runtime",
  "title": "Voice Capture Runtime",
  "status": "accepted",
  "phase": "done",
  "progress": 100,
  "goal": "Integrar captura contínua ao voice plane sem quebrar o spool/processador já existentes, usando contrato externo para wake word e VAD.",
  "scope": [
    "config do capture worker",
    "CommandCaptureSource",
    "CaptureWorker com heartbeat, health e status",
    "integração no bootstrap do app",
    "testes do contrato"
  ],
  "done_definition": [
    "worker inicia e para junto do app",
    "GET /v1/voice/capture/status responde",
    "voice_capture aparece no /health",
    "CaptureOnce enfileira clipe detectado no spool",
    "suite relevante passa"
  ],
  "next_actions": [
    "usar esta base como prerequisito do E2E de wake word positivo no deploy"
  ],
  "simulation_commands": [
    "capturador externo retorna stdout vazio e nenhum job é criado",
    "capturador real em silêncio não dispara wake word",
    "capturador externo retorna JSON com detected=true e job é enfileirado",
    "capturador externo falha e voice_capture vai para warning/error"
  ],
  "test_commands": [
    "go test ./internal/voice ./cmd/aurelia -count=1",
    "go test ./... -count=1",
    "bash ./scripts/voice-capture-smoke.sh"
  ],
  "curl_checks": [
    "curl -fsS http://127.0.0.1:8484/v1/voice/capture/status",
    "curl -fsS http://127.0.0.1:8484/health"
  ],
  "fallback_commands": [
    "go run ./cmd/aurelia voice enqueue <arquivo>",
    "voice_capture_enabled=false"
  ],
  "evidence": [
    "CaptureWorker criado em internal/voice/capture.go",
    "capturador real criado em scripts/voice-capture-openwakeword.py",
    "setup isolado criado em scripts/setup-voice-capture-env.sh",
    "bash ./scripts/voice-capture-smoke.sh passou",
    "go test ./... -count=1 passou",
    "worker live ativo em /home/will/aurelia-24x7",
    "/v1/voice/capture/status live responde com status idle e heartbeat fresco",
    "/health live responde voice_capture=ok"
  ],
  "blockers": [],
  "handoff": {
    "owner_engine": "codex",
    "resume_prompt": "A slice ADR-20260319-voice-capture-runtime ja foi aceita. Use-a como base para o E2E positivo de wake word na slice de deploy.",
    "last_updated": "2026-03-19T19:39:00-03:00"
  }
}
