{
  "adr_id": "20260328-smart-router-litellm-gemma3-27b",
  "title": "Smart Router LiteLLM com gemma3 27b como Juiz Soberano",
  "status": "implemented",
  "progress": 100,
  "goal": "Criar configs/litellm/config.yaml com tiers: gemma3:27b (tier 0 local) + OpenRouter fallback (tier 1)",
  "next_actions": [
    "Testar gateway responde em localhost:4000",
    "Validar que gemma3:27b está no tier 0"
  ],
  "handoff": {
    "resume_prompt": "configs/litellm/config.yaml foi atualizado com tiers completos. Próximo passo: testar com LiteLLM rodando.",
    "owner_engine": "claude",
    "last_updated": "2026-03-28T15:00:00Z"
  },
  "test_commands": [
    "go build ./...",
    "curl -s http://localhost:4000/health || echo 'LiteLLM not running'"
  ],
  "curl_checks": [
    "curl -s http://localhost:11434/api/tags | jq '.models[] | select(.name | contains(\"gemma\"))'",
    "curl -s http://localhost:4000/v1/models"
  ],
  "dependencies": [
    "pkg/llm/ollama.go",
    "internal/gateway/provider.go"
  ],
  "blockers": [
    "Environment var LITELLM_MASTER_KEY precisa ser configurada",
    "Environment var OPENROUTER_API_KEY precisa ser configurada"
  ],
  "notes": "P1 - ✅ Concluído. gemma3:27b no tier 0 como Juiz Soberano. config.yaml atualizado com routing_strategy e model aliases."
}
